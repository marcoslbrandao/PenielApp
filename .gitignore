node_modules/
.env
.expo/
dist/
*.log
.DS_Store

# Chave de service account do Google Play (eas submit --platform android)
# — credencial sensível, nunca deve ir pro repositório.
google-play-service-account.json
*service-account*.json

# Arquivos temporarios criados pela camada de montagem quando um arquivo
# aberto e substituido. Nao fazem parte do projeto.
.fuse_hidden*
